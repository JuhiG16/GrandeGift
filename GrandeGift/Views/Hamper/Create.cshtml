@model HamperCreateViewModel

<form enctype="multipart/form-data" asp-controller="Hamper" asp-action="Create" method="post">
    <div class="form-group">
        <input asp-for="Name" id="name" class="form-control" placeholder="Hamper Name" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
    <div class="form-group">
        <textarea asp-for="Details" placeholder="Details" class="form-control"></textarea>
        <span asp-validation-for="Details" class="text-danger"></span>
    </div>
    <div class="form-group">
        <input asp-for="Price" placeholder="AUD" class="form-control" />
        <span asp-validation-for="Price" class="text-danger"></span>
    </div>
    <div class="form-group">
        <select asp-for="CategoryId" asp-items="@(new SelectList(ViewBag.Cats, "Id", "Name"))" class="form-control">
            <option value="Select category" selected disabled>Select category</option>
        </select>
        <span asp-validation-for="CategoryId"></span>
    </div>
    <div class="form-group">
        <select asp-for="Status" asp-items="Html.GetEnumSelectList(typeof(Status))" class="form-control">
            <option value="" selected disabled>Select Status</option>
        </select>
    </div>
    <div class="form-group">
        <div class="custom-file">
            <input class="custom-file-input" id="customFile" asp-for="Photo">
            <label class="custom-file-label" for="customFile">Choose file</label>
        </div>
    </div>
    <button class="btn btn-primary">Create</button>
    @*<button type="button" id="testbtn" class="btn btn-info">Create</button>

        @section Scripts{
            <script type="text/javascript">
                $(document).ready(function () {
                    $('.custom-file-input').on("change", function () {
                        
                        //var fileName = $(this).val().split("\\").pop();
                        //$(this).next('.custom-file-label').html(fileName);
                    })
                });
             </script>
        }*@
</form>

